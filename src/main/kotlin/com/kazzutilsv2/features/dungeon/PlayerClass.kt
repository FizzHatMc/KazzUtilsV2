package com.kazzutilsv2.features.dungeon

class PlayerClass {
/*
    companion object{
        var className = ""
    }

     @SubscribeEvent
     fun onTick(event: ClientTickEvent){
         if(mc.theWorld == null) return
         if(mc.thePlayer == null) return
         if(event.phase != TickEvent.Phase.START) return;

         var classNames : List<String> = mutableListOf(TabUtils.archerName, TabUtils.berserkerName, TabUtils.tankName,TabUtils.healerName,TabUtils.tankName)
         val foundClass = classNames.firstOrNull { it.contains(mc.thePlayer.name) }

         if (foundClass != null) {
             className = foundClass
             //ChatUtils.messageToChat("Classname found '$foundClass'")
         } else {
             //ChatUtils.messageToChat("No class found")
         }
     }

 */

}